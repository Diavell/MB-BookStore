@{
    ViewData["Title"] = "Index";
}
@model PaginatedList<ProductViewModel>

<body style="background-color:antiquewhite">
    <div>
        <div class="row pt-3 pb-3" style="background-color:#FFFFFF; border-radius: 50px">
            <div class="col-md-12">
                <div class="row">
                    <div class="col text-center py-5">
                        <h1 class="text-secondary text-uppercase font-italic common-title">Products</h1>
                    </div>
                </div>
                <div class="row mb-3">
                    <div class="col">
                        <a asp-controller="Products" asp-action="Create" class="btn btn-primary mb-3" style="width: 100%; border-radius: 20px;">Create Product</a>
                    </div>
                </div>
                <div class="row mb-3">
                    <div class="col">
                        <form method="get" asp-controller="Products" asp-action="Index" class="form-inline my-2 my-lg-0 w-100">
                            <input type="text" name="searchQuery" class="form-control mr-sm-2 w-75" style="border-radius: 20px;" placeholder="What are you looking for?" value="@ViewData["CurrentFilter"]">
                            <button type="submit" class="btn btn-secondary w-25" style="border-radius: 20px;"><i class="bi bi-search"></i></button>
                        </form>
                    </div>
                </div>

                <style>
                    .form-inline {
                        display: flex;
                        width: 100%;
                    }

                        .form-inline .form-control {
                            flex: 1;
                            margin-right: 10px;
                        }

                        .form-inline .btn {
                            flex: 0;
                        }
                </style>

                @if (Model.Any())
                {
                    <div>
                        <table class="table table-striped table-bordered results" style="border-radius: 50px; width: 100%;">
                            <thead>
                                <tr>
                                    <th>
                                        <a asp-action="Index" asp-route-sortOrder="@ViewData["IdSortParm"]" class="text-dark">Id                                            
                                            @if (ViewData["CurrentSort"]?.ToString() == "id_desc")
                                            {
                                                <span>&#9660;</span>
                                            }
                                            else if (ViewData["CurrentSort"]?.ToString() == "Id")
                                            {
                                                <span>&#9650;</span>
                                            }
                                            else
                                            {
                                                <span>&#8597;</span>
                                            }
                                        </a>
                                    </th>
                                    <th class="text-dark">Name</th>
                                    <th>
                                        <a asp-action="Index" asp-route-sortOrder="@ViewData["PriceSortParm"]" class="text-dark">Price
                                            @if (ViewData["CurrentSort"]?.ToString() == "price_desc")
                                            {
                                                <span>&#9660;</span>
                                            }
                                            else if (ViewData["CurrentSort"]?.ToString() == "Price")
                                            {
                                                <span>&#9650;</span>
                                            }
                                            else
                                            {
                                                <span>&#8597;</span>
                                            }
                                        </a>
                                    </th>
                                    <th class="text-dark">Description</th>
                                    <th class="text-dark">Category</th>
                                    <th class="text-dark">Author</th>
                                    <th class="text-dark">ISBN</th>
                                    <th class="text-dark">Published Date</th>
                                    <th class="text-dark">Picture</th>
                                    <th class="text-dark">Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var product in Model)
                                {
                                    <tr>
                                        <td>@product.Id</td>
                                        <td>@product.Name</td>
                                        <td>@product.Price</td>
                                        <td>@product.ShortDescription</td>
                                        <td>@product.Category.Name</td>
                                        <td>@product.Feature.Author</td>
                                        <td>@product.Feature.ISBN</td>
                                        <td>
                                            @if (@product.Feature.PublishedDate.Value.ToShortDateString() != "1 Oca 0001")
                                            {
                                                @product.Feature.PublishedDate.Value.ToShortDateString()
                                            }
                                        </td>
                                        <td>
                                            <img class="img-thumbnail" src="@product.StockPictureUrl" alt="@product.Name" style="width: 100px; height: 100px;" />
                                        </td>
                                        <td>
                                            <a asp-controller="Products" asp-action="Update" asp-route-id="@product.Id" class="btn btn-info mb-3 ml-2 rounded-circle custom-btn">
                                                <i class="bi bi-gear"></i>
                                            </a>
                                            <a asp-controller="Products" asp-action="Delete" asp-route-id="@product.Id" class="btn btn-danger mb-3 ml-2 rounded-circle custom-btn"
                                               onclick="return confirmDelete('@product.Id')"><i class="bi bi-trash"></i></a>                                            
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                        <nav aria-label="Page navigation example">
                            <ul class="pagination justify-content-center">
                                @if (Model.HasPreviousPage)
                                {
                                    <li class="page-item">
                                        <a class="page-link" asp-action="Index" asp-route-searchQuery="@ViewData["CurrentFilter"]" asp-route-pageNumber="@(Model.PageIndex - 1)" asp-route-sortOrder="@ViewData["CurrentSort"]">Previous</a>
                                    </li>
                                }
                                else
                                {
                                    <li class="page-item disabled">
                                        <a class="page-link" href="#">Previous</a>
                                    </li>
                                }

                                @for (int i = 1; i <= Model.TotalPages; i++)
                                {
                                    <li class="page-item @(Model.PageIndex == i ? "active" : "")">
                                        <a class="page-link" asp-action="Index" asp-route-searchQuery="@ViewData["CurrentFilter"]" asp-route-pageNumber="@i" asp-route-sortOrder="@ViewData["CurrentSort"]">@i</a>
                                    </li>
                                }

                                @if (Model.HasNextPage)
                                {
                                    <li class="page-item">
                                        <a class="page-link" asp-action="Index" asp-route-searchQuery="@ViewData["CurrentFilter"]" asp-route-pageNumber="@(Model.PageIndex + 1)" asp-route-sortOrder="@ViewData["CurrentSort"]">Next</a>
                                    </li>
                                }
                                else
                                {
                                    <li class="page-item disabled">
                                        <a class="page-link" href="#">Next</a>
                                    </li>
                                }
                            </ul>
                        </nav>
                    </div>
                }
                else
                {
                    <div class="alert alert-info">
                        Product not found
                    </div>
                }
            </div>
        </div>
    </div>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script type="text/javascript">
        function confirmDelete(productId) {
            return confirm("Are you sure you want to delete the product with ID " + productId + "?");
        }
    </script>
</body>
